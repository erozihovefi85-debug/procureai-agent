FROM node:18-alpine

WORKDIR /app

# 复制 package files
COPY backend/package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制源代码
COPY backend/src ./src

# 创建上传目录
RUN mkdir -p uploads

# 暴露端口
EXPOSE 3001

# 启动应用
CMD ["node", "src/index.js"]
